export interface Schedule {
    id: string
    address: string
    amount: string
    category: string
    container_amount: number
    date: string
    material: string
    material_amount: number
    schedule_date: string
    status: string
}